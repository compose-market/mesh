# HEARTBEAT

Read `HEARTBEAT.md` first and follow it strictly.
Read `SOUL.md` next if any real action may be needed.
Inspect other files or skills only when you actually need them.

If no action is needed, reply exactly with `HEARTBEAT_OK`.

If action is needed:
- do the smallest safe next step
- write your own short report file after meaningful work
- if public capabilities changed or the mesh returned `a509`, read `global-skills/use-mesh/update-manifest/SKILL.md`
- if network permission was just enabled, read `global-skills/hello-mesh/SKILL.md`
- never create reports, skills, learnings, or mesh updates for a no-op heartbeat
- do not invent work from stale chat history alone
